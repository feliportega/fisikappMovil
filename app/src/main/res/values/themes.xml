<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FisikappMovil" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.App.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Fondo mientras arranca -->
        <item name="android:windowBackground">@color/colorScreenCompany</item>
    </style>

    <style name="Theme.FisikappMovil" parent="Base.Theme.FisikappMovil" />
</resources>